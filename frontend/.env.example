# ============================================================
#  前端环境开关
#
#  这是模板文件。它会被提交到 Git，供组员参考。
#  真正生效的文件叫 .env（不提交，因为被 .gitignore 排除了）。
#
#  怎么用：把本文件复制一份，改名为 .env 即可（Windows 下：
#  在文件管理器里复制粘贴，重命名时连同 .example 一起删掉）。
#
#  ⚠️ 改完必须重启 npm run dev 才生效（vite 不热更新配置文件）。
# ============================================================

# 【最重要的一行】是否使用本地模拟数据
#   不写这一行 = 走模拟数据（推荐，后端没好时用这个）
#   false      = 走真实后端接口（同学 A 的 Flask 起来之后改成 false）
VITE_USE_MOCK=true

# 接口前缀。前端发 /api/register，
# 开发时由 vite.config.js 的 proxy 转发到 Flask。
VITE_API_BASE=/api
